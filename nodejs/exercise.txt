1) create nodejs server which returns name of states in given zone 
    zone can be south north east west 

    example 
    http://localhost:5000/west 
        gujarat
        rajasthan
        maharastra 

2) create nodejs server which returns name of players of 2026 in given ipl team 
    GT 
    shubhnam gill, rashid khan etc 

3) redevelop example 1 and return it as single line html output

4) redevelop example 2 and return it as single line html output

5) develop example which returns your resume as html page. 

6) create local module that has function getSquare and getQube which return square and qube of given numbers. export it and use it in another javascript file 

7) create local module which has getSimpleInterest() and getCompoundInterest() function which return simple & compound interest and it in another javascript file.

8) Create restaurant site which should have 5 pages (using bootstrap do not use ai ) & load the page from files using fs & url module. if user send request for not existing page display 404 page.  
    1) home 
    2) Menu 
    3) Gallery 
    4) Book hall 
    5) contact us 